---
name: fusion-ux-contract
description: UX Designer 专用。将线框图转化为 UI_CONTRACT.md，对前端开发的约束力等同于 INTERFACE.md 对后端的约束力。
---

# fusion-ux-contract — UI 契约写作

> **融合来源**: fusion-workflow Stage 0.5（UI_CONTRACT.md 是对前端的约束，等同于 INTERFACE.md 对后端的约束力）

---

## ⚡ 执行前 FP 两问（强制）

1. **我们的目的是什么？**
   → 将线框图"翻译"为前端可直接执行的开发规范，让 fe-ui-builder 读完 UI_CONTRACT 就能动工，无需再问 UX Designer 任何问题。
2. **这些步骤已经不可原子级再分了吗？**
   → 逐屏输出契约，每个屏幕一个章节，不合并处理。

---

## 前置验证

必须确认以下文件已就绪：

```
[x] pipeline/0_5_prototype/Feature_Screen_Map.md
[x] pipeline/0_5_prototype/User_Flow.md
[x] pipeline/0_5_prototype/Wireframes/（至少一张审查后版本存在）
[x] pipeline/0_5_prototype/Wireframes/[屏幕名]-notes.md（每屏注释）
```

---

## 执行序列

### Step 1: 汇总所有屏幕（来自 Feature_Screen_Map）

列出屏幕目录，确认每个屏幕的 notes.md 都已存在。

### Step 2: 逐屏写出 UI_CONTRACT 章节

每个屏幕输出一个标准章节，格式如下：

```markdown
## 屏幕: [屏幕名称]

**对应 F-ID**: F1.1, F1.2
**用户角色可见性**: 普通用户（未登录时不可见）
**入口路径**: 登录页 → 点击「注册」→ 本屏幕

### 布局结构

| 区域 | 区域名称 | 内容描述                        | 交互规则           |
| ---- | -------- | ------------------------------- | ------------------ |
| 顶部 | 页头     | Logo + 返回链接                 | 点击 Logo → 回首页 |
| 主体 | 注册表单 | 字段：用户名/邮箱/密码/确认密码 | 提交前校验所有字段 |
| 底部 | 辅助链接 | "已有账号？去登录"              | 点击 → 跳转登录页  |

### 组件清单

| 组件ID   | 组件名称 | 描述         | 状态变体              |
| -------- | -------- | ------------ | --------------------- |
| C-REG-01 | 输入框   | 用户名输入   | 默认/聚焦/错误/成功   |
| C-REG-02 | 提交按钮 | 完成注册     | 默认/加载中/禁用      |
| C-REG-03 | 错误提示 | 字段校验失败 | 显示（红色文字）/隐藏 |

### 状态规则

| 状态       | 触发条件         | 用户看到什么                               |
| ---------- | ---------------- | ------------------------------------------ |
| 初始状态   | 页面加载完成     | 空白表单，提交按钮灰色不可点               |
| 填写中     | 用户输入任意字段 | 提交按钮变为可点击蓝色                     |
| 提交中     | 点击提交         | 按钮变为「注册中…」+旋转图标，表单不可编辑 |
| 注册成功   | 服务端确认       | 跳转到「注册成功」页                       |
| 注册失败   | 字段格式错误     | 对应字段下方显示红色错误说明               |
| 邮箱已注册 | 服务端返回冲突   | 邮箱字段下方显示「此邮箱已被注册」         |

### 导航规则

| 操作           | 跳转目标            | 条件   |
| -------------- | ------------------- | ------ |
| 注册成功       | 注册成功页 / 主界面 | 无条件 |
| 点击返回       | 上一页              | 无条件 |
| 点击「去登录」 | 登录页              | 无条件 |
```

### Step 3: 写全局契约规则（跨屏幕通用约定）

```markdown
## 全局契约

### 加载状态标准

- 所有异步操作（数据加载/提交）期间：操作按钮禁用 + 显示旋转图标
- 加载超过 3 秒：显示提示文字（具体文案见各屏幕描述）

### 空状态标准

- 列表为空时：显示图标 + 说明文字 + （可选）引导操作按钮
- 不允许显示空白区域（空白 = 用户不知道发生了什么）

### 错误提示标准

- 字段级错误：显示在字段正下方，红色文字
- 全局错误（操作失败）：顶部 Toast 提示，3 秒后自动消失
- 错误文字必须是用户能理解的自然语言，不出现错误码

### 文字规范

- 所有按钮文字：动词开头（"登录" / "保存" / "删除"）
- 所有提示文字：完整句子，不用技术术语
- 占位符文字（Placeholder）：说明预期格式（如"example@email.com"）
```

---

## 产出物

```
pipeline/0_5_prototype/UI_CONTRACT.md
```

文件结构：

```markdown
<!-- Author: UX-Designer -->

# UI_CONTRACT.md — 前端开发约束文档

> 本文件对前端开发的约束力等同于 INTERFACE.md 对后端的约束力。
> fe-ui-builder 读完本文件即可独立开始开发，无需再问 UX Designer。

## 全局契约

[见上方全局规则]

## 屏幕契约

### 屏幕1: 登录页

[见上方屏幕章节模板]

### 屏幕2: 注册页

[...]

## F-ID 覆盖矩阵

| F-ID | 功能名称 | 体现在哪个屏幕/组件                         |
| ---- | -------- | ------------------------------------------- |
| F1.1 | 用户登录 | 登录页 → C-LOGIN-01（登录表单）             |
| F1.2 | 密码找回 | 登录页（弹层）→ C-LOGIN-02（找回链接+弹层） |
| ...  | ...      | ...                                         |
```

---

## 质量闸门

- [ ] 所有屏幕均有对应契约章节
- [ ] 每个屏幕的组件清单已完整（含所有状态变体）
- [ ] 状态规则覆盖正常流 + 异常流（不只有"成功"状态）
- [ ] F-ID 覆盖矩阵完整（每个 F-ID 都有对应组件引用）
- [ ] 全局契约已写入（加载/空状态/错误标准）
- [ ] 零技术术语（全文扫描）

**自检通过 → 通知 UX Consultant 启动对抗审查。**
